services:
  scout:
    image: ghcr.io/<your-org>/scout:master
    container_name: scout
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:10020:10020"
    volumes:
      - ./data:/app/data
      - ./storage:/app/storage
